import CreateProjectForm from "@/components/CreateProjectForm";

const ProjectPage = () => {
    return (
        <div>
          <CreateProjectForm/>
        </div>
    );
};

export default ProjectPage;